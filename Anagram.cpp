#include<iostream>
using namespace std;
int main()
{
    char str[20],str1[20];
    int i,j,l1=0,l2=0,count=0;
    cin>>str;
    cin>>str1;
    l1=strlen(str);
    l2=str(str1);
    if(l1!=l2)
    {
        cout<<"No"<<endl;
    }
    else
    {
        for(i=0;i<l1;i++)
        {
            for(j=0;j<l2;j++)
            {
                if(str[i])
            }
        }
    }
}
